spring:
  application:
    name: eureka-ha

server:
  port: 8761

eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka/
      #default-zone: http://peer2:8762/eureka,http://admin:admin@peer1:8761/eureka
    register-with-eureka: false
    fetch-registry: false
  instance:
    hostname: zws-one

